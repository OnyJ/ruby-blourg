{
  "name": "tech-challenge-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "./*"
  ],
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "test:unit": "yarn workspaces foreach run test:unit",
    "test:e2e": "yarn workspaces foreach run test:e2e"
  },
  "devDependencies": {
    "@captive/ts-config": "^1.1.0",
    "@types/jest": "^28.1.3",
    "@types/node": "^16.0.0",
    "es-jest": "^2.0.0",
    "jest": "^28.1.1",
    "typescript": "4.x"
  },
  "packageManager": "yarn@3.2.1"
}
